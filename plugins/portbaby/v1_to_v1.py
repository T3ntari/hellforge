"""v1 Machine ↔ v1 Human conversion. Already handled by events_to_e_source()."""

def convert(events, bpm, source_text=""):
    """Convert between v1 machine and human (returns same events, different text).
    Actual conversion is in events_to_e_source() — this is a passthrough."""
    return events
