/** PLACEHOLDER — owned by tickets-ts-chrome (Footer.tsx). Replace wholesale.
 *  Contract: no props. Persistent shortcut bar at the bottom row. */
import { Box, Text } from "ink";

export default function Footer(): JSX.Element {
  return (
    <Box>
      <Text color="gray">Footer</Text>
    </Box>
  );
}
