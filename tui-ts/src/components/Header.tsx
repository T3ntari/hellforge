/** PLACEHOLDER — owned by tickets-ts-chrome (Header.tsx). Replace wholesale.
 *  Contract: no props. "HELL'S CODE" red banner + "- T3ntari" grey. */
import { Box, Text } from "ink";

export default function Header(): JSX.Element {
  return (
    <Box>
      <Text color="gray">Header</Text>
    </Box>
  );
}
